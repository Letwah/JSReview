// export const genRandomId = () => {
//   return Math.round(Math.random() * 100000);
// };

export const random = () => {
  return Math.round(Math.random() * 100000);
};

export default "hello";

//also can mix them - defaults and named
